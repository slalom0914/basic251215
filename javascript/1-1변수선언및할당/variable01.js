//사용자에게 입력을 받아서 처리(비즈니스 로직-Model계층)한다.
//처리한 결과를 출력(React)해 준다.

let a = 2
console.log(a)

const b = 3
//b = 5
console.log(b)
console.log("안녕")

// 컴퓨터 = 하드디스크 + 연산 + 메모리
// 메모리(Memory) - 1byte가 여러 개 쌓여 있는 구조

// App = Code + Data + Stack + Heap

// 함수의 실행 순서를 보관하는 곳이 Stack
// Heap에는 객체가 보관

// npm install nodemon -g 엔터
// 코드의 변경 사항을 지속적으로 인지 결과 반영이 즉시 이루어짐
// -g 옵션 : global하게 설치하기 이다.(특정 프로젝트에 한정되지 않고 전체에서 사용이 가능함.)